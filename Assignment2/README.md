This code requires the gem rgl, version 0.5.7
(https://github.com/monora/rgl)
I have installed it using:
sudo gem install rgl

Command to run the code:


Command to run the tests:
ruby AnnotationTest.rb

